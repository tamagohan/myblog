<h1>Editing account</h1>

<% form_for(@account, :url=>account_path) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :locals=>{:f=>f} %>
  <p>
    <%= f.submit '更新' %>
  </p>
<% end %>

<%= link_to 'トップ', user_path %>

